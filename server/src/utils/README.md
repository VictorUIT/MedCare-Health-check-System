# Shared utilities

Pure, reusable helpers belong here instead of controllers or services.

- `appointmentUtils.ts`: appointment code generation
- `dateUtils.ts`: date-only parsing and formatting
- `scheduleUtils.ts`: weekday validation, date mapping and time-slot generation
